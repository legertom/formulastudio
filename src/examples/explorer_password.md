# Password Strength Logic

This example demonstrates a complex nested logic structure for determining password strength.

{{if and greater len pass 8 and contains pass "#" not contains pass "password" "Strong" "Weak"}}
